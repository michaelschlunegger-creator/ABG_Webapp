# ABG Web App Architecture

Gmail / Google Mail -> Web App -> (MDM-GPT, SharePoint, Cataloguer)
Cataloguer -> PROSOL (manual) -> Web App -> SharePoint + Gmail

This MVP keeps orchestration in backend service layer with mock-mode adapters.
